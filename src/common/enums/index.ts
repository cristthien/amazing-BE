export enum Gender {
  Male = 'M',
  Female = 'F',
  Other = 'O',
}
export enum UserRole {
  Admin = 'admin',
  User = 'user',
}
export enum AuctionStatus {
  PENDING = 'pending', // chuẩn bị bắt đầu
  ACTIVE = 'active', // đang
  CLOSED = 'closed', // kết thúc
}
